version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r synergyyai-core/requirements.txt
run:
  command: gunicorn --bind 0.0.0.0:8080 --chdir synergyyai-core main:app
  network:
    port: 8080
