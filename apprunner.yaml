version: 1.0
build:
  commands:
    build:
      - pip3 install -r synergyai-core/requirements.txt
run:
  runtime: python311
  command: /usr/local/bin/uvicorn synergyai-core.main:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
