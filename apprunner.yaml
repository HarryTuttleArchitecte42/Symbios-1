version: 1.0
build:
  commands:
    build:
      - pip3 install -r synergyai-core/requirements.txt --target /app/deps
run:
  runtime: python311
  command: python3 /app/start.py
  network:
    port: 8080
